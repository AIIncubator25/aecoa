# 🔑 Secrets (never commit)# 📁 Uploaded files (keep local - may contain sensitive data)
uploads/
# Keep uploads directory structure but ignore all contents
!uploads/
uploads/*
!uploads/.gitkeep.env
.streamlit/secrets.toml

# ✅ Example secrets can be tracked (safe)
# Keep this in repo so others know the format
!.streamlit/secrets.example.toml

# ✅ (Optional) Track non-secret Streamlit config:
# If you want to keep config.toml in Git, uncomment the 2 lines below:
#.streamlit/*
#!.streamlit/config.toml

# 🐍 Python bytecode/cache
__pycache__/
*.py[cod]
*.pyo

# 📊 Streamlit local cache & static build outputs
.streamlit/cache/
.streamlit/static/

# 🗂️ Virtual environments
.venv/
venv/
env/

# � Uploaded files (keep local - may contain sensitive data)
uploads/

# �📝 Logs
*.log
compliance_analysis.log

# macOS / Windows system files
.DS_Store
Thumbs.db
desktop.ini
